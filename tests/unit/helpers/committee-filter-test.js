
import { committeeFilter } from 'legislator-lookup/helpers/committee-filter';
import { module, test } from 'qunit';

module('Unit | Helper | committee filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = committeeFilter([42]);
  assert.ok(result);
});

